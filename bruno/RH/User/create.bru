meta {
  name: create
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/app-user
  body: json
  auth: inherit
}

body:json {
  {
    "email" : "cc.com",
    "password" : "root"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
