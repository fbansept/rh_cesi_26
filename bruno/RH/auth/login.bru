meta {
  name: login
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/login
  body: json
  auth: inherit
}

body:json {
  {
    "email" : "a@a.com",
    "password" : "root"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
